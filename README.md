# cssChangeByJS
Wanted to make something so the css rules can be changed without going by `<element query from document>.style.<style property of whatever kind>`, which clutters the html tag.

For now, focusing on the `<style>` tag, hoping to add `<stylesheet>` once kinks have been worked out.
